- Color switcher fails if one faction unpacked the other not, home system tile probably.

- Alliance Promissory should be ignored when the "FACTION Alliance" card is present.  Use the active/idle state for the ref card instead of the linked commander.
